<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN HTTP pairing (PC on home network). Prefer Tailscale HTTPS later. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
